[project]
name = "kg-agent"
version = "0.1.0"
description = "A knowledge graph used as an agent's world model, planner, and constraint on hallucination"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = ["pytest>=8"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
